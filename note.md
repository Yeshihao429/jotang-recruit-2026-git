# note
## 任务一
- 我使用了Claude Code
- 在Windows终端使用指令原生安装，使用CC-Switch配置了deepseek的api。简单学习了一下快捷键。
- 让它帮我实现了一个简易的纯html番茄钟，然后我手动创建了一个github仓库，让它帮我自动写好README、commit以及.gitignore本地配置文件并push。之前用claude做过一个有记账和日历功能的微信小程序，然后也上线了，但是每月数据库收费高加上没多少人用，就暂时停用了，现在开发版还能使用日历功能，记账功能因为依赖数据库所以停用了。https://github.com/Yeshihao429/-pomodoro-timer.git。
- 对于番茄钟项目，claude写好之后用Coze分析了一下可以优化的地方，然后交回给claude进行优化，主要改了一下计时准确性、数据持久化、后台提醒功能等
- git diff后，它修改了按钮等地方的代码，红色的表示删除，绿色的表示增加。
- 因为提示词给的到位，这个文件它第一次写的就基本没有bug，改了一下之后没有明显的体验差异，不过做出的这些改动确实是影响到了细节体验，总体上来说很好。
- btw:使用同样的提示词让Codex生成了一份，但是效果没有claude给力。

## 任务二
- 与普通AI的区别： 
1. 普通聊天AI的输出只能是纯文本，而agent可以调用工具输出结构化的json指令
2. 普通聊天AI跑在公司的云端服务器上，在严格的沙盒中，没有本地系统的访问权限，agent在安装时就被授予了本地系统的访问权限
3. agent遵循ReAct循环，分析需求-调用工具-获得搜索结果-决定下一步-执行修改
4. agent读文件时可以动态截取文件内容，直接注入到上下文中

- tool的作用：给了tool，大模型可以上网获取信息，并且可以直接上手改文件，还能精确知道自己所处的环境
- 员工手册：给agent在这个项目中的永久角色设定、行为边界、规则，定义说话风格，禁止危险操作。
- 遗忘：大模型本身有上下文Token上限，然后agent区分短时记忆和长期记忆。并且上下文越多每次执行任务消耗的额度越多，agent越“笨”，所以使用的时候可以手动定期清理上下文，比如claude我就让它自己给自己配了一个占用的进度条，到80%以上就手动一个compact。
- 额度：token计费（一个单词拆分成两三个token），调用次数计费，隐藏消耗（向量检索、工具调用、多步思考）
- 风险：破环本地环境、泄露隐私，横向渗透、入侵内网，植入木马、挖矿。
